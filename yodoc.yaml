---
# yodoc
# https://github.com/suzuki-shunsuke/yodoc
src: src
dest: dest
tasks:
  - name: gh version
    action:
      run: |
        echo "$FOO"
        pwd
      dir: src
      # script: gh_version.sh
      env:
        FOO: foo
    checks:
      - exit_code: 0
